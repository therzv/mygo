package floor

import (
	"fmt"
)

func ShowAdultCompany(s string, n int) {
	for i := 0; i < n; i++ {
		fmt.Println(s, i)
		//a := fmt.Sprintf("ini rizvi punya string s %s ke %d ", s, i)
		//fmt.Println(a)
	}

}
